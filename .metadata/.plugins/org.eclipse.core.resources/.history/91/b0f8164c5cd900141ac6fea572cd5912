package com.epam.cdp.byta2015.module3.lecture2.tourist.datareaders;

import java.io.File;
import java.util.List;
import java.io.FileReader;
import java.io.FileWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.io.BufferedReader;

import com.epam.cdp.byta2015.module3.lecture2.tourist.model.Offer;

public class TxtFileWriter extends AbstractWriter{

	private String fullPath;
	private Offer offer;
	
	public void TxtFileWriter(Offer offer, String fullPath) {
		super();
		this.fullPath = fullPath;
	}
		
	@Override
	public void writeInfo() {
		File f = new File(getFullPath());
		BufferedReader br = null;
		String str = null;
		Offer offer = new Offer();
		/*
			try {
				BufferedWriter brOut = new BufferedWriter(new FileWriter(myFile, true));
				
				brOut.write("\n" + typeOfTour + ": " + transport + ": " + food + ": " + daysCount + "\n");
				brOut.flush();
				brOut.close();
			} catch (IOException e) {
				System.out.println("Error during file writing: " + e);                           
			}
	*/	
		}
		
	
	
	public String getFullPath() {
		return fullPath;
	}

	public void setFullPath(String fullPath) {
		this.fullPath = fullPath;
	}

}
